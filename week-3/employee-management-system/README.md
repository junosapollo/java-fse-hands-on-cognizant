# Week 3 — Employee Management System

A compact Spring Boot/JPA application covering entity relationships, derived
queries, JPQL, projections, validation, pagination, auditing timestamps, a
batch `EntityManager` service, and an explicitly named second JDBC datasource.

Run `../../mvnw -Dmaven.repo.local=../../.m2/repository -pl week-3/employee-management-system test`.
