# Week 3 — Spring Boot application

This is the Spring Core/Maven exercise 9 implementation: a Spring Boot library application with Spring Web, Spring Data JPA, H2, an entity/repository, and CRUD REST endpoints under `/books`.

```bash
../../mvnw -pl week-3/library-management-boot test
```
